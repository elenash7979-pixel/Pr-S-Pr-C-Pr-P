const tenseRules={
 'Present Simple':{use:'Use it for habits, routines, facts and permanent situations.',form:'Subject + base verb. Add -s/-es after he, she or it.',positive:'Luna explores the castle every night.',negative:'Luna does not explore the castle every day.',question:'Does Luna explore the castle every night?',clues:'always, usually, often, sometimes, never, every day'},
 'Present Continuous':{use:'Use it for actions happening now or around the present moment.',form:'Subject + am / is / are + verb-ing.',positive:'Luna is flying through the city now.',negative:'Luna is not flying slowly.',question:'Is Luna flying through the city now?',clues:'now, right now, at the moment, Look!, Listen!'},
 'Present Perfect':{use:'Use it for past actions connected to now, results and life experience.',form:'Subject + have / has + past participle.',positive:'Luna has found the magic compass.',negative:'Luna has not found the invisible hat yet.',question:'Has Luna found the magic compass?',clues:'already, just, yet, ever, never, before, so far'}
};
const rulesDialog=document.createElement('dialog');rulesDialog.id='rulesDialog';rulesDialog.innerHTML='<article class="rules-card"><button class="rules-close" aria-label="Close rules">×</button><div id="rulesContent"></div></article>';document.body.append(rulesDialog);rulesDialog.querySelector('.rules-close').onclick=()=>rulesDialog.close();rulesDialog.onclick=event=>{if(event.target===rulesDialog)rulesDialog.close()};
document.querySelectorAll('.tense-pills span').forEach(pill=>{const name=pill.textContent.trim();pill.innerHTML=`<b>${name}</b><small>RULES · PRESS</small>`;pill.tabIndex=0;pill.setAttribute('role','button');pill.onclick=()=>openTenseRules(name);pill.onkeydown=event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();openTenseRules(name)}}});
function openTenseRules(name){const rule=tenseRules[name];document.querySelector('#rulesContent').innerHTML=`<p class="rules-label">GRAMMAR GUIDE</p><h2>${name}</h2><section><h3>When do we use it?</h3><p>${rule.use}</p></section><section><h3>Sentence structure</h3><p>${rule.form}</p></section><div class="rule-examples"><div><span>＋ AFFIRMATIVE</span><p>${rule.positive}</p></div><div><span>− NEGATIVE</span><p>${rule.negative}</p></div><div><span>？ QUESTION</span><p>${rule.question}</p></div></div><section class="clue-box"><h3>Time clues</h3><p>${rule.clues}</p></section>`;rulesDialog.showModal()}
